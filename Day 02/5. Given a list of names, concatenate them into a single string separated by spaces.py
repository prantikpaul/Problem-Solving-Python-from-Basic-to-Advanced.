#Given a list of names, concatenate them into a single string separated by spaces

def all_str (s):

    rr=pp.replace(' ','')
    return print(rr)

pp = input('Enter list of names and separate by space : ')

all_str(pp)

